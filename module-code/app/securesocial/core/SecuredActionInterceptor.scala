package securesocial.core

import play.api.mvc.{Session, Request}

/**
 *
 */
trait SecuredActionInterceptor {
//  def onLogin(request: Request, identity: Identity): Option[Session]
//  def onLogout(request: Request, identity: Identity): Option[Session]
//  def onSignUp(request: Request, identity: Identity): Option[Session]
}
